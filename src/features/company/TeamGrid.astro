---
import { getCollection } from "astro:content";
import Icon from "../../components/ui/Icon";

const teamMembers = await getCollection("team");

const iconMap: Record<string, string> = {
    linkedin: "Linkedin",
    github: "Github",
    twitter: "Twitter",
    email: "Mail",
};
---

<section class="py-24 bg-surface-page">
    <div class="container mx-auto px-4">
        <div class="text-center mb-16">
            <h2
                class="text-accent-500 font-bold tracking-widest uppercase text-xs mb-4"
            >
                Nuestro Equipo
            </h2>
            <h3 class="text-3xl md:text-5xl font-bold text-brand-500 mb-6">
                Expertos en Gestión Documental
            </h3>
            <p class="text-neutral-600 max-w-2xl mx-auto text-lg font-light">
                Profesionales comprometidos con la transformación digital de su
                empresa.
            </p>
        </div>

        <div
            class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 max-w-7xl mx-auto"
        >
            {
                teamMembers.map((member) => (
                    <div class="group">
                        <div class="bg-white p-8 rounded-2xl border border-neutral-100 hover:border-neutral-200 hover:shadow-xl transition-all duration-300 text-center h-full flex flex-col">
                            <div class="mb-6">
                                <div class="w-32 h-32 rounded-full mx-auto bg-gradient-to-br from-brand-500 to-brand-ink-900 flex items-center justify-center border-4 border-surface-page group-hover:border-accent-500 transition-colors duration-300 group-hover:scale-105 transform">
                                    <span class="text-white text-4xl font-bold">
                                        {member.data.avatarUrl}
                                    </span>
                                </div>
                            </div>
                            <h4 class="text-xl font-bold text-brand-500 mb-2">
                                {member.data.name}
                            </h4>
                            <p class="text-accent-500 font-semibold mb-4 text-xs uppercase tracking-widest">
                                {member.data.role}
                            </p>
                            <p class="text-neutral-500 text-sm leading-relaxed mb-6 flex-grow">
                                {member.data.bio}
                            </p>
                            {member.data.social &&
                                member.data.social.length > 0 && (
                                    <div class="flex justify-center gap-4 pt-4 border-t border-neutral-100">
                                        {member.data.social.map((link) => (
                                            <a
                                                href={link.url}
                                                target="_blank"
                                                rel="noopener noreferrer"
                                                class="w-8 h-8 rounded-full bg-brand-500/5 text-brand-500 hover:bg-brand-500 hover:text-white transition-colors flex items-center justify-center p-1.5"
                                                aria-label={link.platform}
                                            >
                                                <Icon
                                                    name={
                                                        iconMap[link.platform]
                                                    }
                                                    className="w-full h-full"
                                                    client:only="react"
                                                />
                                            </a>
                                        ))}
                                    </div>
                                )}
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>

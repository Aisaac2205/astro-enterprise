---
// src/components/ui/PageHero.astro
interface Props {
    badge?: string;
    title: string;
    titleAccent?: string;
    description: string;
    bottomOverlayClass?: string;
}

const {
    badge,
    title,
    titleAccent,
    description,
    bottomOverlayClass = "from-surface-page",
} = Astro.props;
---

<section class="relative bg-brand-ink-900 py-32 overflow-hidden">
    <!-- Grid background -->
    <div
        class="absolute inset-0 bg-[linear-gradient(rgba(246,107,14,0.03)_1px,transparent_1px),linear-gradient(90deg,rgba(246,107,14,0.03)_1px,transparent_1px)] bg-[size:50px_50px]"
    >
    </div>
    <div class="container mx-auto px-4 relative z-10">
        <div class="max-w-4xl mx-auto text-center">
            {
                badge && (
                    <h1 class="text-accent-500 font-bold tracking-widest uppercase text-xs mb-6">
                        {badge}
                    </h1>
                )
            }
            <h2
                class="text-4xl md:text-6xl font-bold text-white mb-6 leading-tight"
            >
                {title}
                {
                    titleAccent && (
                        <span class="text-accent-500">{titleAccent}</span>
                    )
                }
            </h2>
            <p class="text-neutral-400 text-xl font-light max-w-2xl mx-auto">
                {description}
            </p>
        </div>
    </div>
    <!-- Gradient overlay bottom -->
    <div
        class={`absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t ${bottomOverlayClass} to-transparent`}
    >
    </div>
</section>
